## 2024-06-21 - [ARIA Live Regions]
**Learning:** For elements that dynamically update over time (like the alert log list in EdgeSight dashboard), adding `aria-live="assertive"` and `role="log"` ensures that screen readers announce these crucial updates.
**Action:** When creating dynamic status logs, implement ARIA live regions as a standard pattern to increase accessibility.
